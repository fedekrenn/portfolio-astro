<section id="contact" class="sub-section">
  <h2>Contacto</h2>
  <form action="https://formspree.io/f/xdorqrzz" method="POST">
    <label for="contacName">
      Nombre:
      <input
        type="text"
        name="contacName"
        id="contacName"
        placeholder="Juan Perez..."
        autocomplete="name"
        required
      />
    </label>
    <label for="email">
      Email:
      <input
        type="email"
        name="email"
        id="email"
        placeholder="user@email.com"
        autocomplete="email"
        required
      />
    </label>
    <label for="message">
      Mensaje:
      <textarea name="message" id="message" cols="30" rows="10" required></textarea>
    </label>
    <div>
      <input type="submit" value="Enviar" class="btn" />
      <input type="reset" value="Borrar" class="btn"/>
    </div>
  </form>
</section>

<style>
  section.sub-section {
    margin-bottom: 0;
  }

  form {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 30px;
    background: linear-gradient(45deg, #ffffff54, #3f50eba6);
    border-radius: 20px;
    width: 60%;
    margin: 0 auto;
    padding: 30px 0;
    min-height: 70vh;
  }

  label {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    width: 100%;
  }

  input,
  textarea {
    border: none;
    border-radius: 10px;
    padding: 10px;
    width: 80%;
    outline: none;
    background-color: #f3f3f3;
    font-family: var(--font-family);
    font-size: 1em;
  }

  input:focus,
  textarea:focus {
    outline: 2px solid rgb(235, 89, 89);
  }

  div {
    display: flex;
    justify-content: space-around;
    width: 80%;
    gap: 10px;
    margin-top: 20px;
  }
</style>
